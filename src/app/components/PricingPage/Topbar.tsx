import { PricingTopbar } from '~/app/components/shared/Topbar';

/**
 * Re-export of PricingTopbar from the shared Topbar folder
 * This maintains backward compatibility while moving the component to the unified location
 */
export { PricingTopbar as Topbar };
